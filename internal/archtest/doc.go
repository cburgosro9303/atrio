// Package archtest enforces Atrio's unidirectional package dependency rules as
// an executable test rather than as prose in a document.
//
// It holds no production code: the rules live entirely in imports_test.go.
package archtest
